[Unit]
Description=The TLS version bumping proxy
After=network.target nss-lookup.target

[Service]
Type=simple
ExecStart=/usr/local/bin/tlsproxy -cert /etc/tlsproxy/cert.pem -key /etc/tlsproxy/key.pem -server bbc.co.uk:443 -listen 0.0.0.0:9000

[Install]
WantedBy=multi-user.target
